# Production Environment Configuration
# Copy this file to .env and update with your actual values
# WARNING: All values marked as "REQUIRED" must be changed from defaults

# Server Configuration
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# Database Configuration (PostgreSQL REQUIRED for production)
# REQUIRED: Replace with your actual PostgreSQL connection string
DATABASE_URL="postgresql://prod_user:secure_password@your-db-host:5432/memory_jar_production"

# Twilio Configuration (REQUIRED for WhatsApp integration)
# REQUIRED: Get these from your Twilio Console
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_actual_twilio_auth_token
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Mem0 Configuration (REQUIRED for memory storage)
# REQUIRED: Get your API key from mem0.ai
MEM0_API_KEY=your_actual_mem0_api_key
MEM0_BASE_URL=https://api.mem0.ai

# OpenAI Configuration (REQUIRED for audio transcription)
# REQUIRED: Get your API key from OpenAI
OPENAI_API_KEY=sk-your_actual_openai_api_key

# Logging Configuration
LOG_LEVEL=warn
LOG_FILE=logs/app.log

# Security Configuration (REQUIRED)
# REQUIRED: Generate a secure 32+ character JWT secret
JWT_SECRET=your_secure_jwt_secret_at_least_32_characters_long_for_production_use
CORS_ORIGIN=https://your-domain.com
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File Storage Configuration
STORAGE_DIR=storage/media

# Health Check Configuration
HEALTH_CHECK_INTERVAL=30000
HEALTH_CHECK_TIMEOUT=5000

# Production Deployment Notes:
# 1. Replace ALL "your_*" values with actual credentials
# 2. Use strong passwords and secure connection strings
# 3. Set up proper SSL/TLS certificates
# 4. Configure firewall rules for database access
# 5. Set up monitoring and alerting
# 6. Configure log rotation and backup strategies
